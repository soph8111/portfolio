/* "use strict";

window.addEventListener("DOMContentLoaded", start);

function start() {
  console.log("start");

  projectHeader();
}

function projectHeader() {
  ScrollTrigger.create({
    start: "top -100",
    end: 99999,
    toggleClass: { className: "--scrolled", targets: ".project_title" },
  });
}
 */
